<UserControl x:Class="Kinect_Middleware.Views.Components.DeviceStatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Kinect_Middleware.Views.Components"
             mc:Ignorable="d"
             d:DesignHeight="60"
             d:DesignWidth="180">


    <UserControl.Resources>
        <local:BoolToColor x:Key="BoolToColor"/>
    </UserControl.Resources>

    <Grid>
        <StackPanel Orientation="Horizontal">
            <Border Width="20" Height="20" CornerRadius="10" BorderThickness="1" BorderBrush="White">
                <Ellipse Width="16" Height="16">
                    <Ellipse.Fill>
                        <SolidColorBrush Color="{Binding Path=Status,
                                                 Converter={StaticResource BoolToColor},
                                                 RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=UserControl}}"/>
                    </Ellipse.Fill>
                </Ellipse>
            </Border>

            <TextBlock HorizontalAlignment="Left"
                       VerticalAlignment="Center"
                       Foreground="White"
                       Margin="10">
                <Run Text="{Binding Path=Text,
                            RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=UserControl}}"/>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
