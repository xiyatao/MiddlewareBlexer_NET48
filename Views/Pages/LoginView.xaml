<UserControl x:Class="Kinect_Middleware.Views.Pages.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Kinect_Middleware.Views.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" VerticalAlignment="Center">
            <TextBox Name="UsernameField"
                         VerticalContentAlignment="Center"
                         FontSize="15"
                         Width="280" 
                         Height="30"
                         Margin="10"/>
            <PasswordBox Name="PasswordField"
                             VerticalContentAlignment="Center"
                             FontSize="15"
                             Width="280" 
                             Height="30"
                             Margin="10"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Name="LoginButton"
                            Content="{DynamicResource BtnLogin}"
                            Click="Login_Click"
                            Width="80" 
                            Height="25"
                            Margin="10"/>

                <Button Name="LogoutButton"
                            Content="{DynamicResource BtnLogout}"
                            Click="Logout_Click"
                            Width="80" 
                            Height="25"
                            Margin="10"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="1"
                    HorizontalAlignment="Right">
            <Button Name="WebsiteButton"
                    Content="{DynamicResource BtnWebsite}"
                    Click="GoToWebsite_Click"
                    Width="80" 
                    Height="25"
                    Margin="10"/>
        </StackPanel>
    </Grid>
</UserControl>
