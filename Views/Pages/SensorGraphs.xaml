<UserControl
    x:Class="Kinect_Middleware.Views.Pages.SensorGraphs"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Kinect_Middleware.Views.Pages"
    xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="500"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="ChartPanelStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Margin" Value="0,10,0,20" />
            <Setter Property="Background" Value="#333333" />
            <Setter Property="Padding" Value="20" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect
                        BlurRadius="8"
                        Opacity="0.2"
                        Color="White" />
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer Background="#252525" VerticalScrollBarVisibility="Auto">
        <StackPanel
            Margin="20"
            Background="#252525"
            Orientation="Vertical">

            <!--  Accelerometer Chart  -->
            <Border Style="{StaticResource ChartPanelStyle}">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,10"
                        FontSize="20"
                        FontWeight="Bold"
                        Foreground="#7ba1c7"
                        Text="Accelerometer (X, Y, Z)" />
                    <lvc:CartesianChart Height="250" Series="{Binding AccelSeries}">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Title="Time" />
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Title="Acceleration" />
                        </lvc:CartesianChart.AxisY>
                    </lvc:CartesianChart>
                </StackPanel>
            </Border>

            <!--  Heart Rate Chart  -->
            <Border Style="{StaticResource ChartPanelStyle}">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,10"
                        FontSize="20"
                        FontWeight="Bold"
                        Foreground="#e3766b"
                        Text="Heart Rate" />
                    <lvc:CartesianChart Height="200" Series="{Binding HeartRateSeries}">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Title="Time" />
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Title="BPM" />
                        </lvc:CartesianChart.AxisY>
                    </lvc:CartesianChart>
                </StackPanel>
            </Border>

            <!--  RR Interval Chart  -->
            <Border Style="{StaticResource ChartPanelStyle}">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,10"
                        FontSize="20"
                        FontWeight="Bold"
                        Foreground="#6be397"
                        Text="RR Interval" />
                    <lvc:CartesianChart Height="200" Series="{Binding RRSeries}">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Title="Time" />
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Title="RR (ms)" />
                        </lvc:CartesianChart.AxisY>
                    </lvc:CartesianChart>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
